apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: doctor-clinic
spec:
  type: LoadBalancer
  selector:
    app: doctor-clinic-nginx
  ports:
    - port: 80
      targetPort: 80
